#include <bits/stdc++.h>

using namespace std;

template <class T>
T sum(T a, T b) {
    return a+b;
}

int main() {
    double a = 0.5, b = 0.6;
    cout << sum(a, b) << '\n';
    pair<int, string> p;
    p = {8, "ahmed"};
    cout << p.first << ' ' << p.second << '\n';
    return 0;
}
