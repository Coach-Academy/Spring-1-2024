#include <bits/stdc++.h>

using namespace std;

using ui = unsigned int;

int32_t main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  int tc;
  cin >> tc;
  while (tc--) {
    ui x;
    cin >> x;
    cout << (~x) << '\n';
  }
}
