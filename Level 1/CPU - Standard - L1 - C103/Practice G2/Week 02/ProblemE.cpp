#include <bits/stdc++.h>
using namespace std ;
int main () {
   double n , m , a;
   cin >> n >> m >> a;
   double length = ceil (n / a) ;
   double width = ceil (m / a);
   cout <<   (long long) length * (long long ) width ;


}

